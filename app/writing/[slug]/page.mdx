import WritingPost, { generateMetadata, generateStaticParams } from './page-helper';

export { generateMetadata, generateStaticParams };

export default function Page(props) {
  return <WritingPost {...props} />;
}
